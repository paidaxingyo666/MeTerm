<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the main MeTerm.app, required for Apple
  notarization (Developer ID distribution). WKWebView's JS engine needs JIT and
  unsigned executable memory; disable-library-validation lets the app load the
  bundled Finder Sync extension / any non-team-signed dylibs. No App Sandbox —
  this is Developer-ID (outside the App Store), and the app needs full PTY/FS
  access for a terminal.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
