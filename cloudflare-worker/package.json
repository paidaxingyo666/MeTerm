{
  "name": "meterm-updater-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test update-worker.test.mjs",
    "check": "node --check update-worker.js && node --test update-worker.test.mjs"
  }
}
