# 项目改动规则（Project Rules）

## 作用范围
- 本文件作用于整个项目：`/home/paidaxing/meterm`
- 后续所有代码与配置改动均需遵循本规则

## 规则 1（强制）
- 所有改动不得影响应用在 macOS 下的功能、行为、显示与交互效果。
- 如果改动必须影响 macOS（包括功能或显示），必须先与项目负责人协商并获得明确同意，再执行改动。

## 执行要求
- 每次涉及跨平台逻辑时，默认优先采用不影响 macOS 的实现方案。
- 若当前环境无法直接验证 macOS，提交说明中必须明确标注该风险与影响面。
